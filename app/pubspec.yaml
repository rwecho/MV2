name: mv2
description: "MV2 — 更好的 V2EX 客户端（Flutter 重写版）"
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
#
# MUST stay ahead of the released MAUI client (latest tag `v1.2.34`) so both
# stores treat this as an upgrade. The legacy CI used
#   CFBundleVersion / versionCode = printf("%d%03d%03d", MAJOR, MINOR, PATCH) + run_number
# e.g. 1.2.34 -> `1002034` + run number, so 1.2.36 -> `1002036xxx`.
#
# The tag `v1.2.36` must match this version — `.github/workflows/publish-flutter.yml`
# fails the build on a mismatch instead of shipping a mislabelled binary.
# Verify against the live versionCode in Play Console / App Store Connect before
# uploading and raise the `+N` if the old run number was 4 digits or more.
version: 2.2.9+2002009000

environment:
  sdk: ^3.13.3

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter

  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^1.0.8
  flutter_riverpod: ^3.4.3
  go_router: ^18.0.1
  dio: ^5.11.1
  shadcn_ui: ^0.56.3
  cached_network_image: ^4.0.0
  shared_preferences: ^2.5.5
  flutter_secure_storage: ^11.1.0
  dio_cookie_manager: ^3.5.0
  cookie_jar: ^4.0.9
  html: ^0.15.7
  # Solana 钱包登录：本地 ed25519 签名（`Sign in to V2EX: <ts>`，见
  # features/auth/data/solana_keypair.dart）。纯 Dart 实现，签名一次的开销
  # 可以忽略，不需要 platform 加速版。
  cryptography: ^2.7.0
  drift: ^2.35.0
  drift_flutter: ^0.3.1
  path_provider: ^2.1.6
  freezed_annotation: ^3.1.0
  json_annotation: ^4.12.0
  url_launcher: ^6.3.2
  share_plus: ^13.3.0
  # 「我的」页给个好评：Play 系统内评分弹窗（iOS 按钮直达商店写评价页，
  # 不走它，见 features/profile/application/app_promotion.dart）。
  in_app_review: ^2.0.10
  image_picker: ^1.2.3
  webview_flutter: ^4.14.1
  app_links: ^7.2.1
  firebase_core: ^4.14.0
  firebase_messaging: ^16.6.0
  firebase_crashlytics: ^5.3.0
  firebase_analytics: ^12.5.0
  package_info_plus: ^10.2.1
  # 内购（永久买断）：RevenueCat 跨商店 SDK。公钥通过 --dart-define 注入，
  # 见 lib/features/pro/application/revenuecat_pro_backend.dart 与 README。
  purchases_flutter: ^10.0.0
dev_dependencies:
  flutter_test:
    sdk: flutter

  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of your
  # package. See that file for information about deactivating specific lint
  # rules and activating additional ones.
  flutter_lints: ^6.0.0
  build_runner: ^2.16.1
  freezed: ^4.0.1
  json_serializable: ^6.14.1
  drift_dev: ^2.35.0

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:

  assets:
    # Mozilla @mozilla/readability@0.6.0 — injected into the WebView by the
    # in-app reader (`lib/features/reader`).
    - assets/vendor/readability.js
    # 登录方式品牌图标（Google / Solana），与官网 `/signin` 同源。
    - assets/auth/

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  # To add assets to your application, add an assets section, like this:
  # assets:
  #   - images/a_dot_burr.jpeg
  #   - images/a_dot_ham.jpeg

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/to/font-from-package

# Local build hook configuration for `package:sqlite3`.
#
# History: this used to force `source: system` (link the OS-provided SQLite)
# because the hook's GitHub-releases download was unreachable from some dev
# machines. That breaks Android hard — apps cannot dlopen the system
# libsqlite3.so, so every Drift/HttpCache call threw at runtime and the home
# feed died on real Android devices while iOS (linkable system lib) was fine.
# The default mode bundles a pre-built SQLite per platform instead; if the
# download fails locally, set up proxy access to github.com rather than
# switching back to `system`.
